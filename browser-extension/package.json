{
  "name": "capture-tool-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Browser extension for Capture Tool — captures DOM metadata during recording sessions.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "chrome-types": "^0.1.327",
    "typescript": "^5.6.0"
  }
}
